begin;

-- place_embeddings_embedding_hnsw_idx가 202608180001에서 만들어졌지만
-- 2026-08-25 확인 시 실제 DB에는 없었다(pg_indexes 조회로 확인 —
-- pkey·content_source_ref_unique·content_id_idx 3개뿐). 2026-08-20 중구
-- RAG 확장 실험(backend/scripts/import_place_embeddings.py) 당시 HNSW
-- 인덱스가 걸린 상태에서 upsert하면 인덱스 갱신 비용 때문에 매 요청이
-- statement_timeout(57014)에 걸리는 문제가 있었고, 이를 우회하려고 인덱스를
-- 지운 뒤 다시 만들지 않은 것으로 보인다(정확한 경위를 남긴 기록은 없음).
--
-- 지금은 57,331건(장소 1,516곳)이 이 인덱스 없이 쌓여 있어, 후보를 먼저
-- content_id로 좁히지 않는 전역 최근접 이웃 검색(§2.10)은 전부 순차
-- 스캔을 탄다. RAG는 아직 추천 파이프라인에 노출되지 않아 지금 당장 장애는
-- 아니지만, 나중에 다시 빠뜨리지 않도록 인덱스 존재 자체를 이 마이그레이션
-- 파일로 남겨 이력을 명시적으로 기록한다.
-- ---------------------------------------------------------------------------
-- 2026-09-14: 이 인덱스를 만들지 않기로 하고 실행을 막았다(D).
--
-- 위 주석이 상정한 것은 "후보를 content_id로 좁히지 않는 전역 최근접 이웃
-- 검색"인데, 그 경로는 지금도 없고 예정에도 없다. 실제 취향 검색
-- (search_place_evidence)은 후보를 반드시 좁혀서 부르고, 그 후보 수는
-- app/recommendation_limits.py의 MAX_RECOMMENDATION_CANDIDATE_LIMIT = 30
-- ("절대 후보 상한", config가 le=30으로 강제)으로 묶여 있다.
--
-- 후보 30곳 = 청크 1,225개이고 EXPLAIN ANALYZE 실측 945ms에 디스크 읽기가
-- 0이다. 인덱스가 해결할 규모의 문제가 생기지 않는다. 오히려 HNSW는 근사
-- 검색이라 지금의 전수 계산보다 정확도가 떨어진다. 속도도 병목이 아니다
-- (LLM 해석 평균 3,536ms vs scoring 834ms, trace_records 실측).
--
-- 덧붙여 현 인스턴스(shared_buffers 224MB) 사양에서는 빌드 자체가 불가능하다.
-- 2026-09-14 시도에서 속도가 294 -> 80 -> 6 행/초로 무너져 18분에 30.8%,
-- 남은 분량 7시간+로 환산돼 취소했다.
--
-- 다시 만들어야 하는 조건은 셋뿐이다.
--   1. 후보 상한을 200곳 이상으로 올릴 때
--   2. 후보를 좁히지 않는 전역 검색 기능이 생길 때 (위 주석이 상정한 경우)
--   3. 임베딩이 지금(220,670조각 / 1,117MB)의 몇 배로 늘어날 때
-- 그때는 컴퓨트 업그레이드가 함께 필요하다.
--
-- 판단 근거 전문: package_D/[기록] place_embeddings HNSW 인덱스를 만들지
-- 않기로 한 판단.md
--
-- 되살리려면 아래 두 줄의 주석만 풀면 된다.
-- ---------------------------------------------------------------------------
-- create index if not exists place_embeddings_embedding_hnsw_idx
--   on public.place_embeddings
--   using hnsw (embedding vector_cosine_ops);

commit;
